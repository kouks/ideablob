import { MutationTree } from 'vuex'
import { State } from './types/State'

export const mutations: MutationTree<State> = {

  /**
   * @param state The current state
   * @param data ...
   */
  mutation (state: State, data: any) : void {
    //
  },

}
